defaults:
  - simulator: shallow_water2d
  - _self_

dataset:
  output_dir: examples/experimental/generated_datasets/shallow_water
  n_train: 200
  n_valid: 20
  n_test: 20

run:
  seed: null
  overwrite: false
