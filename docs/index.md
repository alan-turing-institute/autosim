# AutoSim

AutoSim provides simulator classes and command-line tools for generating
simulation datasets.

Use the tutorials to get started with dataset generation, or use the API
reference to inspect simulator interfaces and implementation details.
